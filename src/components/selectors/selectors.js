export const selectFiltred = (state) => state.todos.filtredTodos;
export const selectTodos = (state) => state.todos.todos;
export const selectLoading = (state) => state.param.isLoading;
export const selectRefresh = (state) => state.param.refreshProducts;
export const selectAlpha = (state) => state.param.isAlpha;
